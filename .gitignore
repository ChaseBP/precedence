# ── secrets: never commit ───────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
keystore/
# Foundry writes broadcast/cache artifacts that can contain sensitive values.
contracts/cache/
contracts/broadcast/


*.log

# ── deps / build ───────────────────────────────────────────────────────────
node_modules/
out/
out-fork/
cache/
.next/
*.tsbuildinfo
contracts/lib/forge-std/

# ── scratch ────────────────────────────────────────────────────────────────
*.tmp

# ── session transcripts: internal, and can contain credentials pasted in chat ──
chat-log.txt
*-log.txt

# ── the nested repo we set aside (empty; kept only so the move is reversible) ──
precedence/.git.disabled-nested-repo/

# ── the latency sampler ────────────────────────────────────────────────────
# `latency.jsonl` IS committed: the front page quotes a distribution from it, and a reviewer has
# to be able to recount the samples. Only the verbose run logs are excluded.
evidence/latency-exp*.log
evidence/.active-collateral-id
evidence/stage-race.log
evidence/prove-race-*.log
evidence/.ux-store.json
# The app's persistent store when PRECEDENCE_STORE_PATH points here. Runtime state, not evidence:
# it churns on every action and holds whatever the machine was last looking at.
evidence/.store.json

# Delivery material for the video, not part of the submission.
DEMO_SCRIPT.md
precedence-full-chat.txt
*.mp4

# ── working documents, kept locally ────────────────────────────────────────
# How the project was built, not what it delivers. `analysis/allocation-divergence.md` is
# deliberately NOT here: two shipped contracts cite it and it documents a real cross-chain
# consistency bug and its fix.
rohan-plan.md
ATTESTCOIN_FACTS.md
RUNNING.md
DEPLOYING.md
PORT_NOTES.md
precedence/AGENTS.md
analysis/attestation-latency-floor.md
analysis/landing-*.md
analysis/live-settlement-layout.md
