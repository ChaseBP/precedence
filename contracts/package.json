{
  "name": "precedence-contracts",
  "private": true,
  "description": "PRECEDENCE Solidity contracts. Built with Foundry — see Makefile (bare `forge` on PATH is NOT Foundry on this machine).",
  "dependencies": {
    "@gluwa/usc-contracts": "0.2.0",
    "@openzeppelin/contracts": "5.1.0"
  }
}
